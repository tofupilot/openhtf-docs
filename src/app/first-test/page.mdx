import { defaultKeywords, defaultOpenGraph } from '../metadata'

export const metadata = {
  title: 'Create Your First Test',
  description:
    'Learn how to install OpenHTF and create your first test script. This guide includes step-by-step instructions to set up a simple test phase and execute it using the command line.',
  keywords: [
    ...defaultKeywords,
    'example',
    'quickstart',
    'snippet',
    'getting started',
    'first test script',
    'create test script',
  ],
  openGraph: {
    ...defaultOpenGraph,
    title:
      'Create Your First Test - Step-by-Step Guide for OpenHTF | OpenHTF Documentation',
    description:
      'Learn how to install OpenHTF and create your first test script. This guide includes step-by-step instructions to set up a simple test phase and execute it using the command line,',
    url: 'https://openhtf.com/first-test',
    images: [
      {
        url: 'https://openhtf.com/first-test-header.png',
        width: 800,
        height: 600,
        alt: 'OpenHTF documentation header showing how to create and run your first test script.',
      },
    ],
  },
}

# Create your first test

Let's create a first test script in under five minutes. {{ className: 'lead' }}

<Image
  src="/first-test-header.png"
  alt="OpenHTF documentation header showing how to create and run your first test script."
/>

## Installation

Install OpenHTF and its dependencies using pip:

```sh
pip install openhtf six setuptools
```

We recommend setting up a [Python virtual environment](https://docs.python.org/3/tutorial/venv.html) to better manage dependencies.

## First run

Create a `main.py` file in your virtual environment and copy this code.

```python {{ title: 'main.py' }}
import openhtf as htf

def hello_world(test):              # Define "hello_world" phase
    print('Hello world!')
    return htf.PhaseResult.CONTINUE # Continue to next phase

def main():
    test = htf.Test(hello_world)    # Define test with hello_world
    test.execute(lambda: "SN1234")  # Execute with DUT serial

if __name__ == '__main__':
    main()                          # Run main function
```

Run it with:

```sh {{ title: 'Terminal' }}
python main.py
```

```txt {{ title: 'Terminal' }}
(venv) C:\tofupilot > python main.py
Hello world!

======================= test: openhtf_test  outcome: PASS ======================
```


This is a very simple OpenHTF test, running through the command line without the Operator UI yet. You'll learn to connect instruments, add measurements, and more in the next sections.
